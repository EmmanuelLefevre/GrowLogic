# Enforces dependency resolution via the lockfile only
frozen-lockfile=true

# Always install exact versions from the lockfile
save-exact=true

# Force a specific driver (Engine Strict)
engine-strict=true
